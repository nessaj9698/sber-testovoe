import { tss } from 'tss-react'

export const useStyles = tss.create({
  backButton: {
    padding: '8px',
    maxWidth: '200px',
    alignSelf: 'start',
    position: 'absolute',
    left: '50px',
  },
})
